name: weekly-report

services:
  mysql:
    image: mysql:8.4
    container_name: weekly-report-mysql
    restart: unless-stopped
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-weekly_report_root_123}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-weekly_report}
      TZ: Asia/Shanghai
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -p$${MYSQL_ROOT_PASSWORD} --silent"]
      interval: 10s
      timeout: 5s
      retries: 12
    volumes:
      - mysql-data:/var/lib/mysql
    networks:
      - weekly-report-net

  weekly-report:
    build:
      context: .
      dockerfile: Dockerfile
    image: weekly-report:latest
    container_name: weekly-report
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
    ports:
      - "${WEEKLY_HOST_PORT:-22080}:8088"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE:-weekly_report}?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_ROOT_PASSWORD:-weekly_report_root_123}
      WEEKLY_REPORT_ROOT: /app
      WEEKLY_FRONTEND_DIST: /app/frontend
      PYTHON_BIN: python3
      WEEKLY_REPORT_PORT: 8088
      WEEKLY_REPORT_EXEMPT_SUBMITTERS: ${WEEKLY_REPORT_EXEMPT_SUBMITTERS:-}
      WEEKLY_AUTH_DEVELOPMENT_MODE: ${WEEKLY_AUTH_DEVELOPMENT_MODE:-false}
      WEEKLY_JWT_SECRET: ${WEEKLY_JWT_SECRET:?Set WEEKLY_JWT_SECRET in the root .env file}
      WEEKLY_JWT_EXPIRE_MINUTES: ${WEEKLY_JWT_EXPIRE_MINUTES:-720}
      WEEKLY_BOOTSTRAP_ADMIN_USERNAME: ${WEEKLY_BOOTSTRAP_ADMIN_USERNAME:-admin}
      WEEKLY_BOOTSTRAP_ADMIN_PASSWORD: ${WEEKLY_BOOTSTRAP_ADMIN_PASSWORD:?Set WEEKLY_BOOTSTRAP_ADMIN_PASSWORD in the root .env file}
      WEEKLY_BOOTSTRAP_ADMIN_REAL_NAME: ${WEEKLY_BOOTSTRAP_ADMIN_REAL_NAME:-系统管理员}
      WEEKLY_DINGTALK_LOGIN_ENABLED: ${WEEKLY_DINGTALK_LOGIN_ENABLED:-false}
      WEEKLY_DINGTALK_CLIENT_ID: ${WEEKLY_DINGTALK_CLIENT_ID:-}
      WEEKLY_DINGTALK_CLIENT_SECRET: ${WEEKLY_DINGTALK_CLIENT_SECRET:-}
      WEEKLY_DINGTALK_REDIRECT_URI: ${WEEKLY_DINGTALK_REDIRECT_URI:-}
      WEEKLY_FRONTEND_URL: ${WEEKLY_FRONTEND_URL:-/}
      WEEKLY_FEEDBACK_ENABLED: ${WEEKLY_FEEDBACK_ENABLED:-true}
      WEEKLY_FEEDBACK_RECIPIENT_NAME: ${WEEKLY_FEEDBACK_RECIPIENT_NAME:-张艺政}
      WEEKLY_FEEDBACK_DINGTALK_USER_IDS: ${WEEKLY_FEEDBACK_DINGTALK_USER_IDS:-}
      WEEKLY_FEEDBACK_DINGTALK_APP_KEY: ${WEEKLY_FEEDBACK_DINGTALK_APP_KEY:-}
      WEEKLY_FEEDBACK_DINGTALK_APP_SECRET: ${WEEKLY_FEEDBACK_DINGTALK_APP_SECRET:-}
      WEEKLY_FEEDBACK_DINGTALK_AGENT_ID: ${WEEKLY_FEEDBACK_DINGTALK_AGENT_ID:-}
      WEEKLY_SUBMISSION_REMINDER_ENABLED: ${WEEKLY_SUBMISSION_REMINDER_ENABLED:-false}
      WEEKLY_SUBMISSION_REMINDER_CRON: ${WEEKLY_SUBMISSION_REMINDER_CRON:-0 0 18 * * SUN}
      WEEKLY_SUBMISSION_REMINDER_ZONE: ${WEEKLY_SUBMISSION_REMINDER_ZONE:-Asia/Shanghai}
      WEEKLY_SUBMISSION_REMINDER_PROCESS_TIMEOUT_SECONDS: ${WEEKLY_SUBMISSION_REMINDER_PROCESS_TIMEOUT_SECONDS:-180}
      TZ: Asia/Shanghai
      JAVA_TOOL_OPTIONS: -Xms128m -Xmx384m
    mem_limit: 768m
    volumes:
      - ./config/.env:/app/config/.env:ro
      - ./output:/app/output
      - ./logs:/app/logs
    networks:
      - weekly-report-net

networks:
  weekly-report-net:
    driver: bridge
    ipam:
      config:
        - subnet: 10.89.77.0/24

volumes:
  mysql-data:
