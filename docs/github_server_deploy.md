# GitHub ? Ubuntu ???????

??????`yzzhang@182.92.251.239`
?????`/data2/person_path/yzzhang/weekly-report`

## 1. ?????

```bash
ssh yzzhang@182.92.251.239
```

## 2. ???????

???????????

```bash
mkdir -p /data2/person_path/yzzhang
cd /data2/person_path/yzzhang
git clone https://github.com/ethan-2937/weekly_report_assistant.git weekly-report
cd weekly-report
```

???????????

```bash
cd /data2/person_path/yzzhang/weekly-report
git pull
```

## 3. ?????????

????????????????????????? `config/.env`?

```bash
cd /data2/person_path/yzzhang/weekly-report
cp config/.env.example config/.env
nano config/.env
chmod 600 config/.env
```

?????

```text
DINGTALK_APP_KEY=??????Client ID?AppKey
DINGTALK_APP_SECRET=??????Client Secret
DINGTALK_AGENT_ID=??????AgentId
DINGTALK_ROOT_DEPT_ID=1
DINGTALK_REPORT_TEMPLATE=??????????
OUTPUT_ROOT=output
```

## 4. ????? Codex skill

???????? Codex ?????? AI ??????????? skill?

```bash
cd /data2/person_path/yzzhang/weekly-report
mkdir -p ~/.codex/skills
rm -rf ~/.codex/skills/weekly-report-assistant
cp -a codex-skills/weekly-report-assistant ~/.codex/skills/weekly-report-assistant
```

## 5. Docker ??

```bash
cd /data2/person_path/yzzhang/weekly-report
docker compose up -d --build
```

?????

```bash
docker compose ps
docker compose logs -f --tail=100
curl http://127.0.0.1:22080/api/health
```

??????

```text
http://182.92.251.239:22080
```

??????????????????????????? TCP `8088`??????????????????????? Nginx Basic Auth?VPN ??????????


## 6. ?????????

???????? `22080-22099` ???????????????? `8088`?? Docker ??????????????? `8088`?

??????????

```text
??????? 22080 -> ?????? 8088
```

?????

```bash
cd /data2/person_path/yzzhang/weekly-report
docker compose up -d --build
```

?????

```text
http://182.92.251.239:22080
```

?? `22080` ??????? `22081-22099` ?????????? `22081`?

```bash
cd /data2/person_path/yzzhang/weekly-report
WEEKLY_HOST_PORT=22081 docker compose up -d --build
```

????????????????

```bash
curl http://127.0.0.1:22081/api/health
```

??????????????????????? Docker Compose ?? `.env` ???

```bash
cd /data2/person_path/yzzhang/weekly-report
echo "WEEKLY_HOST_PORT=22081" > .env
docker compose up -d --build
```

???????? `.env` ?? Docker Compose ??????????? `config/.env`?

## 7. Docker ?????????

???? `docker compose up -d --build` ???

```text
failed to create network weekly-report_default: all predefined address pools have been fully subnetted
```

????Docker daemon ??? bridge ???????????? network ???????????????? `curl 127.0.0.1:8088` ??? `Connection refused`?

????? `docker-compose.yml` ???????????`10.89.77.0/24`?????????????????

```bash
cd /data2/person_path/yzzhang/weekly-report
git pull
docker compose up -d --build
```

?????????????? Docker ???????

```bash
docker network prune -f
docker compose up -d --build
```

`docker network prune -f` ???????????? Docker network????????????????

## 10. ?????????

Web ??????????????????????

```bash
docker exec weekly-report python3 /app/scripts/run_weekly.py --week previous
```

????????????????

```text
/data2/person_path/yzzhang/weekly-report/output/<YYYY-Www>/
```

?? AI ???????? Codex ?? `weekly-report-assistant` skill ???????

```text
output/<YYYY-Www>/summary/manager_report.md
```

## 9. ????

??????????????????

```bash
cd /data2/person_path/yzzhang/weekly-report
git pull
docker compose up -d --build
```

## 10. ??????

```bash
# ????
docker compose ps

# ????
docker compose logs -f --tail=100

# ??
docker compose restart

# ??
docker compose down

# ???????????
docker builder prune
```

## 11. ? sudo / ? Docker ????

???? `docker compose up -d --build` ????

```text
permission denied while trying to connect to the Docker daemon socket
```

?????????? Docker daemon ?????? `sudo` ????????????? `docker` ??????? `/var/run/docker.sock` ???

??????????????

```bash
sudo usermod -aG docker yzzhang
```

?????????????

??????????? Docker ????????????????

```bash
cd /data2/person_path/yzzhang/weekly-report
bash scripts/server_start_no_docker.sh
```

???

```bash
bash scripts/server_stop_no_docker.sh
```

? Docker ???????????? `java`?`mvn`?`node`?`npm`?`python3`?
